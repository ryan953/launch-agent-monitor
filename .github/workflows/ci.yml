name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Show toolchain
        run: swift --version

      - name: Build
        run: swift build -c release

      # There is no test target yet, so the useful check is that the thing the
      # release workflow does still works. Both slices and the lipo are built
      # the same way there; a toolchain or triple problem shows up here rather
      # than half way through cutting a release.
      - name: Build both slices and lipo them
        run: |
          set -euo pipefail
          swift build -c release --triple arm64-apple-macosx14.0
          swift build -c release --triple x86_64-apple-macosx14.0
          mkdir -p dist
          lipo -create -output dist/BGMonitor \
            .build/arm64-apple-macosx/release/BGMonitor \
            .build/x86_64-apple-macosx/release/BGMonitor
          lipo -info dist/BGMonitor
